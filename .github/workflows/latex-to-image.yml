name: LaTeX to Image

# This permission is required so the workflow can push changes back to the repo
permissions:
  contents: write

on:
  push:
    branches: [ "main" ]   # Adjust if your main branch has a different name
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout the repository
      - name: Checkout repository
        uses: actions/checkout@v2
        with:
          # Provide the GitHub token and persist credentials for later commits
          token: ${{ secrets.GITHUB_TOKEN }}
          persist-credentials: true

      # Step 2: Set up Python
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      # Step 3: Install dependencies
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install markdown2
          sudo apt-get update
          sudo apt-get install -y texlive-latex-base texlive-fonts-recommended texlive-fonts-extra texlive-latex-extra dvipng

      # Step 4: Debug - list all .md files (optional, but helpful for verifying your Markdown files)
      - name: Debug - List .md files
        run: |
          echo "Listing Markdown files found:"
          find . -name "*.md" -print

      # Step 5: Run the bracket-based LaTeX-to-image script
      - name: Run LaTeX to Image script
        run: python scripts/latex_to_image.py

      # Step 6: Pull the latest changes to avoid merge conflicts
      - name: Pull latest changes
        run: |
          git pull --rebase origin $GITHUB_REF_NAME || echo "No pull needed"

      # Step 7: Commit and push changes
      - name: Commit and push changes
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add .
          # Avoid errors if no changes are found
          git commit -m "Convert bracket-based LaTeX to images" || echo "No changes to commit"
          git push origin HEAD:$GITHUB_REF_NAME
